# =============================================================================
# Terraform .gitignore
# =============================================================================

# Local .terraform directories
**/.terraform/*

# .tfstate files (remote state should be used)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# Uncomment this if you want to ignore all tfvars files
# *.tfvars
# *.tfvars.json

# Local override files
*.tfvars.local
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Plan files
*.tfplan
tfplan

# Lock file (include in version control)
# !.terraform.lock.hcl

# Ignore the backend-setup state file
# NOTE: You may want to KEEP this file committed as it's the only record
# of the bootstrap resources. Remove this line if you want to track it.
# terraform/shared/backend-setup/terraform.tfstate
# terraform/shared/backend-setup/terraform.tfstate.backup
